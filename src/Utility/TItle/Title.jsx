const Title = ({title}) => {
    return (
        <div className="my-3">
            <h1 className="text-black text-3xl font-semibold dark:text-white">{title}</h1>
        </div>
    );
};

export default Title;